# Detect Breaking Changes - Analysis Report

## Summary

- X total items found
- Breakdown:
  - (... impact level): N items
  - (... more impact levels)
- Files involved:
  - (... glob or file path within repo)
  - (... more files or globs)

## Items by impact level

### (... impact level)

#### (... item name) [(... item category)]

(... description)

(... impacts)

(... impact level reason)

Recommendations:
- (... recommendation for item)
- (... more recommendations)

(... insert more issues)

(... insert more categories)

----

> Detection performed by `devrel-detect-breaking-changes` for:
> - Git repo: `${REPO_URL}`
> - Git tag previous: `${PREV_TAG}`
> - Git tag next: `${EXT_TAG}`
> - Git diff file: `${OUT_FILE_DIFF}`
> - Detection report: `${OUT_FILE_DETECT}`
